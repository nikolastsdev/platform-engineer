apiVersion: v1
kind: Secret
metadata:
  name: ghcr-pull
  namespace: todolist
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: Cg==
# Gerado automaticamente pelo Makefile:
#   make secret REGISTRY_USERNAME=... REGISTRY_PASSWORD=$(GH_TOKEN)
# Substitui o placeholder quando `make apply` roda.
